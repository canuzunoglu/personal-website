import { component$ } from "@builder.io/qwik";

export const Header = component$(() => {
  return (
    <header class="mb-4">
      <h1 class="text-xl dark:text-slate-400">Can Uzunoğlu</h1>
    </header>
  );
})

